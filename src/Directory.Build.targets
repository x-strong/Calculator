<Project>
	<PropertyGroup>
		<DefineConstants Condition="'$(Configuration)'=='Release' and !$(BUILD_SOURCEBRANCH.Contains('canaries'))">$(DefineConstants);PRODUCTION</DefineConstants>
	</PropertyGroup>
</Project>
